import {test} from 'node:test';import assert from 'node:assert/strict';import {normalizedPoint,strokeAppearance,changeHistory} from '../annotations.js';
test('normalized coordinates are independent of page display size and clamp edges',()=>{assert.deepEqual(normalizedPoint({clientX:150,clientY:250},{left:50,top:50,width:200,height:400}),{x:.5,y:.5});assert.deepEqual(normalizedPoint({clientX:250,clientY:450},{left:50,top:50,width:400,height:800}),{x:.5,y:.5});assert.deepEqual(normalizedPoint({clientX:-5,clientY:900},{left:0,top:0,width:200,height:400}),{x:0,y:1})});
test('memorize mode only changes memorize strokes and restores customized appearance',()=>{const s={memorize:true,color:'#ff00ff',opacity:.35};assert.deepEqual(strokeAppearance(s,false),{color:'#ff00ff',opacity:.35});assert.deepEqual(strokeAppearance(s,true),{color:'#000000',opacity:1});assert.deepEqual(strokeAppearance({...s,memorize:false},true),{color:'#ff00ff',opacity:1});assert.equal(s.color,'#ff00ff')});
test('undo redo works across pages; new stroke invalidates redo',()=>{let h={past:[],present:{},future:[]};h=changeHistory(h,{type:'set',value:{1:['a']}});h=changeHistory(h,{type:'set',value:{1:['a'],2:['b']}});h=changeHistory(h,{type:'undo'});assert.deepEqual(h.present,{1:['a']});h=changeHistory(h,{type:'redo'});assert.deepEqual(h.present,{1:['a'],2:['b']});h=changeHistory(h,{type:'undo'});h=changeHistory(h,{type:'set',value:{1:['a','c']}});assert.equal(h.future.length,0)});
